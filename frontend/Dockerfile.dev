FROM node:20

WORKDIR /app

COPY package*.json ./
RUN npm install

# Copy the rest of the app (for initial build, but will be overridden by volume)
COPY . .

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]